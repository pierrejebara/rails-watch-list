

<div id="banner">
  <h2>Save any kind of movies</h2>
</div>
<div class="container">
  <div class="grid">
    <% @lists.each do |list| %>
      <div class= "cards">
        <%= link_to list.name, list_path(list) %>
      </div>
    <% end %>
  </div>
</div>
